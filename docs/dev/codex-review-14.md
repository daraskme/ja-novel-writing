5件の元の再現ケースは解消しています。残る2件は、いずれも公開後の対応でよいと判断します。

- **中：生成記録が不整合になると、判定ログが点検対象から落ちる。**  
  [run_blind_eval.py:526](../../evals/run_blind_eval.py:526)  
  再現：judge完了後、`run.json` の依頼本文だけ変更してreport。`orders` の構築前に抜けるため、判定ログ4本が期待ログから消えました（6本→2本）。  
  修正：集計行の成否に依存せず、割当・実行記録から判定ログを列挙する。

- **低：`--force` でも旧実行の `migrated` が残る。**  
  [run_blind_eval.py:293](../../evals/run_blind_eval.py:293)  
  再現：`migrated` のある実行で `generate --force`。全応答を作り直しても「旧実行器のログから移行した」という説明が残ります。  
  修正：引き継ぎ条件にも `not args.force` を加える。

ファイル変更なし。書き込み処理を差し替えて検証し、保存済み9対の結果・選好の一致も確認しました。全244テストは再実行していません。

公開判定：**可（高の指摘なし。上記の中・低は公開後対応で可）。**