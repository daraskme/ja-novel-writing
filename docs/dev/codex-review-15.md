**条件つき可です。公開前に直すべき点が8件あります。** ファイル変更なし。書き込み不要の117テストと番人214＋62例は通過しました。追加検証は模擬応答・メモリ上で行い、実際の鍵や有料APIは使っていません。

1. **高：Pythonの許可から、書き込み制限と費用上限を迂回できる。**  
   [gateway.py:43](../../evals/gateway.py:43)、[同:128](../../evals/gateway.py:128)。  
   `Bash(python:*)` は、例えば `python -c "from pathlib import Path; Path('../probe.txt').write_text('probe')"` も許す設定です。渡された認証環境変数を使ってPythonから直接APIを呼べば、Claude Codeの予算集計も通りません。`acceptEdits` はPython内部の操作を制限しません。[公式の権限説明](https://code.claude.com/docs/en/permissions)も、ファイル・通信の強制にはsandboxを区別しています。  
   **修正：** 任意Pythonを使わせるならOS側で書き込み・通信を制限し、鍵も道具の実行環境から分離する。費用はGateway側にも制限を置く。「作業ディレクトリ内だけ」というREADME・コメント・テストの説明も直す。タイムアウト時は、外側がgatewayだけを終了してClaudeが残らないよう、子孫プロセスまで終了させる。

2. **高：鍵の伏せ字処理が出力経路を覆っていない。**  
   [gateway.py:85](../../evals/gateway.py:85)、[同:155](../../evals/gateway.py:155)、[同:191](../../evals/gateway.py:191)。  
   ダミー鍵で確認：HTTPエラーに鍵が含まれると、chatの標準エラーと使用量記録の`error`へそのまま出ます。Claudeの成功`result`に鍵が含まれると標準出力へ出て、評価器のログにも残ります。また、道具入力を300字に切ってから伏せるため、切断された鍵の一部が残りました。  
   **修正：** 外部由来の文字列は、切り詰め・JSON化より前に共通の秘匿処理を通す。成功応答、例外、credits、使用量記録も対象にする。

3. **中：`preamble_sha`のない旧記録を、新条件で無検証再利用する。**  
   [run_blind_eval.py:392](../../evals/run_blind_eval.py:392)。  
   同じ生成記録で、古いハッシュを入れると拒否、フィールドを消すと`generation_problem()`が`None`になりました。旧実行の未完了分を再開すると、異なる前置きの応答が混在できます。  
   **修正：** 通常のgenerate再開では欠落を再利用不可にする。旧記録の保存・再判定は「前置き未検証」と明示する別扱いにする。前置き本文・ハッシュをrunにも保存し、過去記録の照合を現在の定数だけに依存させない。

4. **中：「再実行は要らない」が必要な確認まで止める。**  
   [novel_lint.py:1571](../../scripts/novel_lint.py:1571)。  
   FAILなしで字数不足なら、同じ案内に「測り直しはそのあと1度」と「確認のための再実行は要らない」が並びます。WARNの実誤りを直した場合や、通常出力で省略されたINFOの詳細を`--all`で読む場合にも不適切です。  
   **修正：** 「本文を変更せず、全指摘を確認済みなら再実行不要」に限定する。修正後の必要な検算・確認は許す。FAILを読んで確かめる文言自体は問題なし。

5. **中：`--range`の判定単位と表示が一致しない。**  
   [novel_lint.py:1548](../../scripts/novel_lint.py:1548)、[同:1558](../../scripts/novel_lint.py:1558)。  
   1300字のファイル2本に`--range 1200-1500`を指定すると、各ファイルは範囲内なのに、最後は合算して「超過1100字」「削る」と案内します。また`--range 1500`は1690字も範囲内です。下限用の猶予を上限にも加えており、表示する1350〜1650字と食い違います。  
   **修正：** 案内をファイル単位に揃える。上限側の猶予を外し、下限側の追加許容は表示・helpにも明記する。

6. **中：告白の成立条件と終わり方を狭く固定している。**  
   [scene-cards.md:282](../../references/scene-cards.md:282)、[SKILL.md:55](../../SKILL.md:55)。  
   「直球の告白」「胸が熱くなる」だけでは、両思いの成立まで約束したことになりません。「直球なら両思いの返事」とする例は、結末未指定の作品を狭めます。また、相手を見送った後、一人で喜びを噛みしめる結びも成立するため、一人の時間を一律に切る指示は評価例への過剰適合です。明示された保留の契約は優先順位で守られますが、この一般化は残ります。  
   **修正：** 成立を約束した告白に適用を限定する。結びは「約束した充足を保つ」を基準にし、二人のところで切る形は選択肢にする。

7. **中：1.3倍が、具体的な水増し指令になっている。**  
   [short-pipeline.md:90](../../references/short-pipeline.md:90)、[novel_lint.py:1563](../../scripts/novel_lint.py:1563)。  
   「場面を一つ多く」「二往復多く」は作品に必要かを問わない固定割当です。実装でも、1499字に`--range 1500-1500`を指定すると「100字前後を書き足す」と出ました。「LLMの初稿は6〜7割」も今回の観測から一般化しています。  
   **修正：** 1.3倍は不足傾向が確認された環境での任意の見積補正とし、実測目標にしない。固定の場面・往復追加と最低100字を外し、不足量と上限に応じて案内する。

8. **中：評価READMEに因果の断定が残っている。**  
   [README.md:49](../../docs/dev/eval-round3/README.md:49)。  
   「スキルの側の原因は3つ」「初案を疑うが…効いていた」は、作業記録と判定理由から推測した機序を確定しています。生成役の自己説明と一致しても、各指示の因果は分離できません。  
   **修正：** 「原因候補として考えた3点」「記録と整合する仮説」とする。勝率・一般的品質向上・lintと面白さを結び付けない記述は問題なし。

9. **低：C09は百分率を取りこぼし、家庭を指す「うち」を拾う。**  
   [novel_lint.py:1418](../../scripts/novel_lint.py:1418)。  
   実測：`二十枚の25％、つまり十枚だ。`は無検知。`うちの二人の子供が来た。`と`二人で話半分に聞いた。`はINFOです。  
   **修正：** 割合側に`%／％／パーセント`を追加し、「うち」は全体数量との接続を絞る。INFO限定・無検知を検算済みとしない契約は問題なし。公開後対応で構いません。

生成ごとの初期コピーとtreeハッシュの対応、完了順の記録、依頼本文・応答・判定の改変拒否、旧レビューの引用解析とログ監査の修正：**問題なし**。公開済みround3〜5の応答34件も現行の照合を通りました。固定条件の抜き出し、既定字数を下限にしない追記、割合・内訳の検算追加：**問題なし**。全259テストは再実行していません。

**公開判定：条件つき可（公開前に1〜8を修正。9は公開後対応で可）。**