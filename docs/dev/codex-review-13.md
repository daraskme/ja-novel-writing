**条件つき可です。元の再現入力は塞がりましたが、追加ケースで4件の実装上の穴と、READMEの因果表現が残りました。** ファイルは変更していません。

1. **高：依頼本文の変更を生成記録の検証が見逃す。**  
   [run_blind_eval.py:370](../../evals/run_blind_eval.py:370)  
   `run.json` の `prompts[0].text` だけ変更し、ハッシュを残して judge → report。旧依頼への応答が、新しい依頼への応答として判定・集計されます。保存ハッシュ同士しか比較していません。  
   **修正：** `text_hash(prompt["text"]) == prompt["hash"] == g["hash"]` を必須にし、再現テストを追加。

2. **中・新規回帰：変更なしの generate 再開で、有効な判定が「失敗」になる。**  
   [run_blind_eval.py:276](../../evals/run_blind_eval.py:276)  
   generate → judge 完了後、同一条件で generate → report。応答は全件再利用されますが、`run.json` の `judge` が消え、コマンド照合で全判定が失敗になります。  
   **修正：** 再開時に判定コマンドの記録を引き継ぎ、各判定の有効性は既存の照合で判断する。

3. **中・③の残り：空白とタブが混在するコード行を選好として採用する。**  
   [run_blind_eval.py:350](../../evals/run_blind_eval.py:350)  
   `parse_preference("引用:\n\n \t選好: A")` は `A` を返します。先頭の空白が2個・3個でも同様です。  
   **修正：** 先頭の空白・タブを桁数に展開し、4桁以上の字下げをコード扱いにする。

4. **中・⑥の残り：失敗した判定のログを点検対象から落とす。**  
   [run_blind_eval.py:559](../../evals/run_blind_eval.py:559)  
   判定が失敗し、その判定ログが欠落していても、`raw` がないため期待ログに入りません。生成ログだけ残した再現では「2 本を点検して該当なし」となり、判定ログ4本の欠落が消えました。  
   **修正：** 成功した選好ではなく、割当・実行記録から判定ログを列挙する。

5. **中：READMEの「提示順の影響が大きい」は因果を言いすぎ。**  
   [README.md:37](../../docs/dev/eval-round2/README.md:37)  
   各提示順1回ずつでは、順序の効果と判定自体の揺れを分離できません。  
   **修正文：**「9対中4対で、提示順を入れ替えた再判定の選好が変わった。提示順と判定自体の揺れの寄与は、この記録からは分離できない。」

元の①の応答改変拒否、②の判定照合、③のフェンス処理、④の密度、⑤のN07：**問題なし**。READMEの勝率・品質向上・結びの効果・lintと面白さの扱い、移行記録と非公開ログの説明：**問題なし**。

確認済み：書き込み不要の既存111テスト、番人のFAIL禁止214例・維持62例、選好解析の既存ケース。公開18応答・18判定の整合性と、9対の保存済み集計の一致も確認しました。全241テストの再実行はしていません。

公開判定：**条件つき可（上記4件の修正と再現テスト追加、READMEの因果表現修正）。**