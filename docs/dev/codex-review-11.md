公開前に修正が必要です。特に、評価器が**古い判定や失敗した判定を有効な結果として記録する経路**があります。

対象 HEAD は `0561228`。ファイル変更なし。ただし、この環境では `python` が見つからず、指定候補の `python.exe` もアクセス拒否でした。**以下はコードから追跡した再現ケースで、実行結果ではありません。** `--help` は定義を読みましたが、標準入力テスト・221 テストの再実行は未実施です。

1. **高：再開時に生成条件・本文・判定の対応が崩れる**

   [run_blind_eval.py](../../evals/run_blind_eval.py:258) の `cmd_generate()`、`cmd_judge()`、`cmd_report()`。

   再現手順：
   - 同じ `--run` で generate → judge を完了する。
   - generate を `--force` 付きで再実行し、応答本文を変える。
   - judge を `--force` なしで実行する。

   判定ファイルに選好があれば再利用するため、**新しい本文に古い判定が結び付く**。また、同じ系統名のままスキルや生成コマンドを変更すると、旧応答を再利用しながら `run.json` の条件だけ新しくなる。判定コマンドの変更でも同様。

   修正：生成条件・スキルのハッシュ・応答本文のハッシュを保存し、判定を「依頼＋両応答＋割当＋判定条件」のハッシュに結び付ける。不一致なら再利用を拒否する。生成更新時は関連判定を無効化する。

2. **高：終了コードが失敗でも、残った選好を採用する**

   [run_blind_eval.py](../../evals/run_blind_eval.py:208) の `run_once()`、`cmd_judge()`、`cmd_report()`。

   判定役を、次の動作をするコマンドにする。

   ```python
   Path(out).write_text("選好: 差なし\n", encoding="utf-8")
   sys.exit(3)
   ```

   両順序とも失敗するが、`cmd_report()` は終了状態を参照せず本文を読むため、結果を「差なし」にする。次の judge も、この失敗ファイルを完了済みとして飛ばす。

   修正：判定ごとの終了状態・試行履歴・解析成否を保存する。集計と再開は、その成功記録を必須にする。現状では、終了コード 0 の解析不能応答も judge 自体は成功扱いになるため、こちらも失敗として記録する。

3. **中：明示文書の前の導入が、後ろの地の文へ持ち越される**

   [novel_lint.py](../../scripts/novel_lint.py:555) の `document_lines()`。

   ```text
   　メールを開いた。
   ＞了解。
   電話をかけた…
   　雨が降っていた。
   ```

   明示文書で `run` は切るが、`prev` が「メールを開いた。」のまま残る。結果、3 行目を推定文書に含め、N01 FAIL を N09 WARN に下げる。統計上は地の文のままなので、診断との扱いも分かれる。

   修正：明示文書との境界で導入状態も破棄する。`paras` に入らない `＞` 単独行も境界として扱う必要がある。

4. **中：文書内の文字欠落・印の残留が verify を通る**

   [export.py](../../scripts/export.py:486) の `canonical_lines()`、`project_source()`、`verify()`。

   次はいずれもコード上 `ok=True` になる。

   ```python
   o = export.Options(target="plain", indent="keep")
   export.verify(["＞## 件名"], "# 件名", o)       # # が欠落
   export.verify(["＞＊ ＊"], "＊", o)            # 文面の ＊ が欠落

   o = export.Options(target="plain", scene_break="＞＊")
   export.verify(["＞＊"], "＞＊", o)             # 内部の印が残留
   ```

   原稿側で明示文書を識別しても、後段で種類を失い、文面を見出し・場面転換として正規化してしまう。

   修正：検証用の原稿表現にも文書の種類を保持し、文面には見出し段数・場面転換記号の正規化を適用しない。出力側との対応付けでも同じ区別を維持する。

5. **中：除去した見出しを挟む二つの文書がつながる**

   [export.py](../../scripts/export.py:356) の `convert_text()`、`apply_blank_policy()`。

   ```text
   ＞一通目。
   # 次の手紙
   ＞二通目。
   ```

   `heading="drop"`、`blank="none"`／`"dialogue"`／`"para"` で、出力は次になる。

   ```text
   一通目。
   二通目。
   ```

   見出しの除去時に文書境界も失われ、verify も通る。通常の空行を挟んだ二文書を守る処理だけでは足りない。

   修正：見出しを出力から除いても、境界情報または文書ブロック ID を残し、空行方針に渡す。

6. **中：記号中心の明示文書が解析から落ちる**

   [novel_lint.py](../../scripts/novel_lint.py:383) の `Doc.__init__()`、`collect_stats()`、`check_notation()`。計数側は [count_chars.py](../../scripts/count_chars.py:188)。

   再現ケースは三つ。

   - `＞……`：raw／narou／body は **2 字**、lint は **0 字**で M01 FAIL。
   - `＞**！**`：段落から落ちるため、文書内の Markdown を N07 が検知しない。
   - 次の入力：`＞＊` が消えるため、`same_ending_max_run` が **4**になる。契約上は **2**。

   ```text
   雨が降っていた。
   彼は傘を開いた。
   ＞＊
   道は濡れていた。
   駅は遠かった。
   ```

   修正：本文計数からの除外と、境界・記法検査からの除外を分ける。空の明示文書や飾り行も境界として保持し、N07 は字数対象への採否に依存させない。沈黙表現の計数基準も lint と body で統一する。

   なお、**N07 は現行実装では WARN**。今回の問題は FAIL の降格ではなく、診断そのものの消失。

7. **中：隔離の説明が、実際の保証より強い**

   [run_blind_eval.py](../../evals/run_blind_eval.py:165) の `stage_arm()`、`cmd_generate()` と [evals/README.md](../../evals/README.md:40)。

   - 生成役の両系統は同じ一時親ディレクトリ配下にある。スキルなし側から `../../*/ja-novel-writing/SKILL.md` を探索できる配置で、元リポジトリの絶対パスを知る必要すらない。
   - `--run` の内部配置チェックは現行 `SKILL_DIR` だけ。別版を `--arm old=C:/old-skill` とした場合、`--run C:/old-skill/references/blind-run` が許される。再実行では、その過去結果をスキル側へコピーできる。
   - 判定役にも、ホスト上の結果・割当・ログへの読み取りを遮断する処理はない。

   修正：少なくとも全系統の入力ルートに対して `--run` を検査する。厳密な隔離を主張するなら、各役の読み取り可能範囲を制限する実行環境が必要。

   但し書きは、生成役だけでなく判定役も含め、次が適切です。

   > 作業ディレクトリと依頼本文を分離していますが、ファイルの読み取り隔離は保証していません。他系統、元リポジトリ、結果・割当・ログ、利用環境の設定へアクセスできる可能性が残ります。由来情報をプロンプトに直接含めない比較であり、厳密な盲検を保証するものではありません。

8. **中：引用した選好を、判定役自身の選好として読む**

   [run_blind_eval.py](../../evals/run_blind_eval.py:319) の `parse_preference()`。

   ```text
   今回は結論を出せません。
   応答からの引用：
   > 選好: A
   ```

   現在は `A` を返す。コードフェンス内の `選好: A` も拾う。

   修正：引用・コードブロックを除外し、指定された最終判定行を解析する。有効な最終判定がなければ、途中の一致へ戻らず解析失敗にする。

9. **低：N10 が、無効化した FAIL を根拠に出る**

   [novel_lint.py](../../scripts/novel_lint.py:690) の `check_notation()`。

   ```python
   cfg.overrides = {"N01": {"off": True}}
   text = "メッセージが届いた。\n待ってる…"
   ```

   N01 は出ないが、内部の `n01` 配列から N10 を作るため案内が出る。「N01〜N04 の FAIL があるときだけ」という今回の条件に反する。

   修正：実際に有効な FAIL 診断の行番号から候補を絞る。

   また、`メールを読んだが窓を開いた。\n外は雨…` も導入と誤認する。これは INFO に限定した推定の限界として扱えるが、否定例の追加は有用。

10. **低：ライブラリ直呼びでは BOM・改行の正規化が揃わない**

    [export.py](../../scripts/export.py:356) の `convert_text()`／`verify()` と、count_chars の各計数関数。

    ```python
    s = "\ufeff＞本文"
    o = export.Options(target="plain", indent="keep")
    out, _ = export.convert_text(s, o)
    export.verify([s], out, o)
    ```

    印が残ったまま一致する。三計数関数も **4 字**、`novel_lint.Doc(s).total_chars` は **2 字**。CRLF を直接渡すと raw が CR も数える。

    CLI の読み込み経路では正規化されるため、CLI の不具合ではない。修正は、公開関数の入口で正規化するか、「正規化済み文字列が前提」と明文化して共通入口を提供すること。

問題なしと判断した項目（静的確認の範囲）：

- 自動降格設定を作らず、B 案を INFO の案内だけにした設計：問題なし。
- 導入後の空行を一つ許す設計：問題なし。
- `keep` で外周空行を追加しない設計：問題なし。
- 明示文書を地の文・台詞統計から外し、総字数と全文密度へ含める設計：問題なし。
- 飾り行を body から除く方針：問題なし。ただし上記の実装不一致あり。
- C 群の検算対象を明示記号だけで免除する処理：問題なし。
- 通常のエスケープ、`＞＞`、字下げ後の `＞`、CLI 経由の BOM・CRLF：問題なし。
- `＞` だけのファイルを本文ゼロとして扱うこと：問題なし。
- 正しい入力・固定した作品対に対する A/B 反転と `settle()`：問題なし。
- `judge_request()` への系統名・版・履歴・lint・期待出力の直接混入：問題なし。ファイル経由の隔離は上記の制約あり。
- notation.md と SKILL.md の任意性・keep・直接投稿への注意：問題なし。
- results.md の勝率・因果・lint と面白さに関する制限：問題なし。

公開判定：**条件つき可（上記の高・中の修正と、再現テスト・既存221テストの実行確認が条件）**。