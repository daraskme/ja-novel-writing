# 開発資料（スキルの実行時には読まれない）

- `design-spec.md` 初稿の設計仕様（用語、担当範囲、6 原則、scripts の契約）。初稿以後の変更は反映していない。
- `threshold-calibration-aozora33.md` lint 警報線の根拠。青空文庫 33 作品の計量と、ソース間の数値の裁定。計測スクリプトは `threshold_measure.py`。
- `policy-decisions.md` 設計方針の食い違い 8 論点の裁定（NG 語を執筆時に見せるか、五感、感情、レビュー出力、章末、会話タグ、約物、改稿）。
- `platform-format-verification-2026-09.md` 投稿サイトの記法・字数・AI 申告、公募規定の確認記録（2026-09 時点）。
- `codex-review-1.md`〜`codex-review-10.md` 外部モデル（Codex CLI / gpt-6-astra）によるレビュー全文。10 回目の判定は「出荷可」。
- `eval-round1/` スキルあり／なしの生成 4 組と、由来を伏せた読み比べの記録。
- `eval-round2/` 実行器（`evals/run_blind_eval.py`）を使った読み比べの計画と結果。
- `eval-round3/` 別の系列のモデルでの再判定と、Claude Code を生成役にしたラウンド 3〜5（Vercel AI Gateway 経由。`evals/gateway.py`）。
- `consult-1-remaining-issues.md` 残っていた 2 つの課題（字下げなし原稿の作中文書、評価）についての Codex との相談。
- `codex-review-11.md`〜`codex-review-14.md` その実装の検証レビューと、修正の確認（14 回目で「可」）。
- `codex-review-15.md` 評価ラウンド 3〜6 を根拠にしたスキルと道具の変更のレビュー。
- `eval-round6/` 未知の依頼での比較、連載の食い違いの修正、所要時間を縮める試み（ラウンド 6〜9）。
- `consult-2-loop-and-confession.md` 継ぎ足しの周回と直球の告白についての Codex との相談。`research-grok/` は同じ問題を Grok 4.6 に検索つきで調べてもらった結果（出典の実在確認は一部だけ）。
- `eval-round11/` その結論を入れた版の比較（未使用の依頼 6 件。判定役 2 つが正反対の選好を示した）と、人間による読み直し 2 回。
- `consult-3-reduce-rules-before-draft.md` 読み直しで選ばれなかった 3 依頼についての Codex との相談（文言を足すのをやめ、初稿の前に読む規則を減らす）。
- `eval-round13/` その構造の変更（作風の道を二つに分け、娯楽の掌編は `flash-experience.md` だけを読む）の確認と、複数の読者に回す読み比べシートの説明。
- `consult-4-second-reader.md` 共有シートの二人目の読者の回答を受けた Codex との相談（評価の本文から取った例を外す、書いた後の照合を具体化する）。反映した変更と、今回書く範囲と読者の既知を決める手順は `eval-round13/README.md`。
- `codex-review-16.md` その変更のレビュー（条件つき可）と、直した点。

## レビューの経過（2026-09-19〜20）

| 回 | 主な指摘 | 対処 |
|---|---|---|
| 1 | 汎用規則が「抑制された写実」へ均す。検算を教える例が計算を誤る。C 群の誤 FAIL。パックに契約が入らない。keep 比率がノルマ化 | SKILL.md と prose-core を選択型に。例文修正。scripts 修正 |
| 2 | 否定・疑問の誤 FAIL、未来の人物変化の漏出、予算の測り方、keep 条件が狭すぎ、結びの文言が強すぎ | C 群を共通判定に書き直し。パックの必須ブロック化 |
| 3 | 敬体の否定・疑問、括弧始まりの字下げ、英文行の脱落、必須資料なしでパックが成功 | FAIL を断定の形に限定。パックは欠落で停止 |
| 4 | 伝聞・思い込み・仮定・台詞内・記号入りの語の誤 FAIL、書名や固有名の N04 | ブラックリストをやめ、断定と読める型だけ FAIL（ホワイトリスト） |
| 5 | 引用より前の文脈、記号を含む語の数え方、入れ子の引用 | 引用の前も見る。数え方 3 通りを受理。引用を台詞／それ以外に分類 |
| 6 | 比較・否定の述語（超える・満たない・あらず）、外側が『』の台詞、行末の題名・看板、手紙の項目行 | FAIL を同格形と、コピュラで言い切る主題形の 2 形に限定 |
| 7 | 同格形のあとの任意述語（否定した・撤回した）、次の文での訂正、別行の題名、句読点つきの作中文書 | 述語を閉じた集合に。訂正の先読み。作中文書のまとまりは WARN。**運用を「FAIL も直す前に読んで確かめる」に変更** |
| 8 | 7 回目の誤 FAIL は全解消。残る誤 FAIL は読めば分かる種類。停止理由は、参照手順に残る旧指示（FAIL は必ず直す／提出条件は FAIL 0／即修正）と新契約の矛盾 | short-pipeline・longform・notation を新契約に統一。SKILL.md の「lint の読み方」を書き直し。JSON にも注意を同梱。部分操作・後続文の否認・半角濁点などを改善 |
| 9 | 契約と衝突する記述があと 2 箇所（revision.md の「新しい FAIL が無いこと」、ai-tells.md の C01 処方） | 修正文案どおり置換。保証表現（WARN になる）を「検知できた場合は」に。部分操作・否定の射程・発話を導く地の文を改善 |
| 10 | **出荷可**。契約矛盾なし。残る誤 FAIL は前後を読めば判断でき、新契約で扱える | 出荷後でよいとされた点のうち、別文で残字を示す部分操作・区間の表記（東京―名古屋）・表現の統一を実施 |
| 11 | 作中文書の ＞ 記法と評価器の実装レビュー（条件つき可）。高: 評価器が、作り直した応答に古い判定を結び付ける／失敗で終わった判定の選好を採用する。中: 明示文書の前の導入が後ろの地の文へ持ち越される、検証が文面の「## 件名」「＊ ＊」を均して欠落を見逃す、落とした見出しを挟む 2 文書がつながる、記号だけの文面が境界と N07 から落ちる、隔離の説明が保証より強い、引用された「選好: A」を読む。低: 止めたルールを根拠に N10、関数直呼びの BOM・CRLF | 全件を修正し、再現入力をテストに追加（`test_document_notation.py` の CodexReview11、`test_blind_eval.py`）。応答と判定を条件のハッシュに結び付け、成否を記録。検証は文面を字面のまま比較。沈黙だけの行の字数を count_chars と統一。隔離は「保証しない」と明記し、ログの事後点検を追加。Codex 側の環境では python を実行できず、指摘は静的な追跡によるもの（こちらで全件を実行して再現を確認） |
| 12 | レビュー 11 の修正の確認（条件つき可）。11 の 2〜5・9・10 は塞がったと確認。残り: generate を挟まずに書き換えた応答を judge・report が採用する、report が判定コマンドの不一致と判定本文の書き換えを見逃す、閉じていないコードフェンスと字下げコードの中の選好を読む、沈黙の行が密度の分母だけ増やして K02・K00 を消す、記号だけの原稿で N07 が出ない、ログが無いのに点検「該当なし」と書く | 全件を修正し、再現入力をテストに追加（CodexReview12、test_blind_eval.py）。生成記録の検証を generate・judge・report で共通化、判定に本文のハッシュ、フェンスを行単位で追跡、沈黙の行を全文検索にも入れ K00 の切り替えをルールごとの分母に、N07 を本文字数のガードから独立、ログの点検を 3 状態に。評価ラウンド 2 の記録は「旧実行器による移行記録」と明記（Codex は原ログで対応が確かめられるなら再実行不要と判断） |
| 13 | レビュー 12 の修正の確認（条件つき可）。12 の 6 件の再現入力は塞がったと確認。残り: run.json の依頼の本文だけを書き換えると検証をすり抜ける、同じ条件で generate を再開すると判定コマンドの記録が消えて有効な判定が失敗扱いになる（修正による回帰）、空白とタブの混ざった字下げのコード行を選好と読む、失敗した判定のログを点検の対象から落とす、評価の README の「提示順の影響が大きい」は因果の言いすぎ | 全件を修正し、再現入力をテストに追加。README は Codex の文案どおり「提示順と判定自体の揺れの寄与は分離できない」に直した |
| 14 | **可**（高の指摘なし）。13 の 5 件は解消。公開後でよいとされた 2 件: 生成の記録が不整合になった対の判定のログが点検の対象から落ちる（中）、`--force` でも旧実行の `migrated` の説明が残る（低） | 2 件とも修正し、テストを追加 |
| 15 | 評価ラウンド 3〜6 を根拠にしたスキルと道具の変更のレビュー（条件つき可）。高: `evals/gateway.py` で、任意の python の許可から書き込みの制限と費用の上限を迂回できる／鍵の伏せ字が出力の経路を覆っていない。中: 前置きの記録が無い旧応答を再開で使い回す、「再実行は要らない」が必要な確認まで止める、`--range` の判定の単位と上限の遊び、「告白が実る山場」が結末未指定の告白まで狭める、「1.3 倍」が水増しの指令になる、評価の README の因果の言い切り。低: C09 の百分率の取りこぼしと「うち」の誤検知 | 全件を修正し、再現入力をテストに追加。gateway は、既定で通す python をスキルの検査スクリプトだけにし、伏せ字をすべての出力の経路に通し、時間切れで子孫のプロセスごと終了。保証できない範囲（OS の隔離ではない、鍵は入れ子の環境にある）を明記。「1.3 倍」は効果も無かったので削除 |
| 16 | 相談 4 の反映と、今回書く範囲・読者の既知を決める手順のレビュー（条件つき可）。作者に渡された設定と読者の既読を混同、語だけの振り分けがモード表や範囲の指定と食い違う、一場面の決着禁止が最終場面や成就の山場を弱める、役割や来歴の全部を実演させる方向、発話分割の件数の取りこぼし、記録で読者の発言・記録者の解釈・相談の判断が混ざる、選好の内訳の誤り | 全件を修正（`codex-review-16.md` の「対処」）。三分類をやめ、範囲（作品全体か一部か）と読者の既知（読了を前提にした既出の本文だけ）で決める形にした。件数は数え直し、スクリプトを記録に置いた。確認のレビュー 17 は条件つき可で、残った中 1 件（直しの方法を台詞・行動に狭める文）も直した |

8 回目は一度 Codex の利用上限で実行できず、統括者が自前で 53 文を探索した（誤 FAIL 0 件）。その後に利用可能になり、8〜10 回目を実施した。

誤 FAIL の回帰は `scripts/tests/test_lint_false_fail_guard.py`（FAIL にしてはいけない文 214 件、FAIL にし続ける基本形 62 件。Codex の 4〜10 回目の入力をすべて含む）。C 群・N 群を触ったら、まずこのテストを見る。

## 残っている課題

- **字下げなしの原稿の中の作中文書**（チャット・メールの本文）。→ 対応済み（2026-09-19、Codex と相談: `consult-1-remaining-issues.md`）。範囲を行頭の全角 `＞` で明示する内部記法を足した（lint は N09 へ回して統計から外し、export は印を外し、count_chars は印を数えない。検証は原稿側でだけ印を外す）。導入文からの推定は、範囲の終わりを決められないので案内（N10 INFO）にとどめ、FAIL の自動降格はしない。無記法のままなら従来どおり運用契約（FAIL も読んで確かめる）で受ける。回帰テストは `scripts/tests/test_document_notation.py`。
- 文脈判定の取りこぼしは原理的に残る。C 群・N 群を触ったら `test_lint_false_fail_guard.py` を先に見て、見つけた誤 FAIL は必ずそこへ足す。
- 評価: ラウンド 2〜9 を実施した（2026-09-19、`eval-round2/`・`eval-round3/`・`eval-round6/` の README）。ラウンド 3 以降は Vercel AI Gateway 経由で、生成役に Claude Code（Sonnet 5）、判定役に gpt-6-astra・Gemini 3.1 Pro・Claude Sonnet 5 を使った。分かったこと: 判定役どうしの不一致が大きく、1 つの判定役の結果は結論にできない／直す前のスキルは、Claude Code が生成役のとき直球の告白で選ばれなかった（返事を保留・条件つき・仕草まかせにしていた）→「告白が実る山場」と初案破棄の「疑わないもの」を追加／スキルの修正に使っていない 5 依頼では、スキルなし版が提示順によらず選ばれた対が、どの判定役にも無かった／連載の依頼は、依頼文の条件違反と、継ぎ足しによる物・状態・時間の食い違いで負けていた → 条件の照合、既定の字数は下限ではない、継ぎ足したら通読、を追加したあとは、スキルなし版が提示順によらず選ばれた対が無くなった。ラウンド 11（`eval-round11/`）: Codex との相談と Grok の調査から、字数は工程で止める（lint は数を報告するだけ、指定を種類で分ける、継ぎ足さずに 1 回書き直す、1 回に書くのは 1 場面、掌編は軽量確認）、削除の基準を「約束した読後感まで保たれるか」に置き換える、を入れた。未使用の 6 依頼では、gpt-6-astra はスキルなし版を 4 件、Gemini はスキルあり版を 4 件選び、正反対だった。目安の字数と指定なしでは測り直しが 2〜3 回に収まったが、守る条件の範囲ではまだ周回する。ユーザーによる読み直しを 2 回実施した（由来を伏せた 7 組と、直した版の 3 組）。第 1 回はスキルあり版 4・スキルなし版 3 で、どの判定役の選好とも合わなかった。スキルなし版が選ばれたのは娯楽の 3 依頼（直球の告白、コメディ、ほのぼのした食卓）で、文言を足して書き直した第 2 回でも 3 組ともスキルなし版が選ばれた。ラウンド 13（`eval-round13/`）: Codex との相談 3 から、文言を足すのをやめ、初稿の前に読む規則を減らした（作風の道を二つに分け、娯楽の掌編は `references/flash-experience.md` だけを読む）。娯楽の依頼で `prose-core.md` を読まなくなったことは確認した。モデルによる出来ばえの判定は実施せず、人間 2 人の読み比べを記録した。複数の読者に回す読み比べシート（ログイン不要、6 組）を作った。所有者の回答では、娯楽の 3 依頼は「同じくらい」2・base 1（コメディは 3 回続けて base で、Claude Code のスキルあり版は 3 回とも同じ骨組みに収束していた）。同じ本文の読み直しでは 3 組中 1 組の選好が変わった。二人目の読者（2026-09-20）とは、不足の指摘が 3 組で重なり（給湯室のスキルあり版の読後感、宿屋の勇者一行らしさ、夏祭りのスキルなし版の台詞の多さ。原因の診断まで一致したわけではない）、選好がそろったのは 2 組。所有者の「地の文の溜め」の好みは二人目では支えられなかった。相談 4 から、評価の本文から取った例を参照から外し、書いた後の照合を具体化し、所有者の求めで、今回書く範囲（作品全体か一部か）と読者が既に知っていることを決める手順を入れた（効果は未確認）。未解決: スキルあり版の所要時間（掌編 1 本に 2〜9 分・道具 8〜41 回。字数の指定があると、初稿が目安の 6 割で出て継ぎ足しの周回になる。文言では動かなかった）、駅前の告白は判定役で割れたまま、結びの文言の効果は未確認、読者 2 人の読み直しを一般化できるかは未確認、創作の出来を指示でどこまで制御できるのかという問い自体。
- lint の警報線のうち、現代エンタメ・Web 向けの列は未較正（`thresholds.json` の `calibrated`）。
- `references/prose-core.md` は約 380 行ある。ラウンド 13 から、読むのは文芸の道の本文を書く前だけにした（娯楽の掌編の初稿の前には読まない）。短い対比例（悪い例）を残しているのは意図した判断だが、読み込み量と誘発のトレードオフは未検証。
- `references/notation.md` の [未]・[二] の項目。投稿・応募の前に公式情報で確かめること。
- `references/revision.md` の処置記録の例に、理由つきで keep した FAIL が残る例を足すと、運用が伝わりやすい。
