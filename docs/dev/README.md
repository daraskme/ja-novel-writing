# 開発資料（スキルの実行時には読まれない）

- `design-spec.md` 初稿の設計仕様（用語、担当範囲、6 原則、scripts の契約）。初稿以後の変更は反映していない。
- `threshold-calibration-aozora33.md` lint 警報線の根拠。青空文庫 33 作品の計量と、ソース間の数値の裁定。計測スクリプトは `threshold_measure.py`。
- `policy-decisions.md` 設計方針の食い違い 8 論点の裁定（NG 語を執筆時に見せるか、五感、感情、レビュー出力、章末、会話タグ、約物、改稿）。
- `platform-format-verification-2026-09.md` 投稿サイトの記法・字数・AI 申告、公募規定の確認記録（2026-09 時点）。
- `codex-review-1.md`〜`codex-review-10.md` 外部モデル（Codex CLI / gpt-6-astra）によるレビュー全文。10 回目の判定は「出荷可」。
- `eval-round1/` スキルあり／なしの生成 4 組と、由来を伏せた読み比べの記録。

## レビューの経過（2026-09-19）

| 回 | 主な指摘 | 対処 |
|---|---|---|
| 1 | 汎用規則が「抑制された写実」へ均す。検算を教える例が計算を誤る。C 群の誤 FAIL。パックに契約が入らない。keep 比率がノルマ化 | SKILL.md と prose-core を選択型に。例文修正。scripts 修正 |
| 2 | 否定・疑問の誤 FAIL、未来の人物変化の漏出、予算の測り方、keep 条件が狭すぎ、結びの文言が強すぎ | C 群を共通判定に書き直し。パックの必須ブロック化 |
| 3 | 敬体の否定・疑問、括弧始まりの字下げ、英文行の脱落、必須資料なしでパックが成功 | FAIL を断定の形に限定。パックは欠落で停止 |
| 4 | 伝聞・思い込み・仮定・台詞内・記号入りの語の誤 FAIL、書名や固有名の N04 | ブラックリストをやめ、断定と読める型だけ FAIL（ホワイトリスト） |
| 5 | 引用より前の文脈、記号を含む語の数え方、入れ子の引用 | 引用の前も見る。数え方 3 通りを受理。引用を台詞／それ以外に分類 |
| 6 | 比較・否定の述語（超える・満たない・あらず）、外側が『』の台詞、行末の題名・看板、手紙の項目行 | FAIL を同格形と、コピュラで言い切る主題形の 2 形に限定 |
| 7 | 同格形のあとの任意述語（否定した・撤回した）、次の文での訂正、別行の題名、句読点つきの作中文書 | 述語を閉じた集合に。訂正の先読み。作中文書のまとまりは WARN。**運用を「FAIL も直す前に読んで確かめる」に変更** |
| 8 | 7 回目の誤 FAIL は全解消。残る誤 FAIL は読めば分かる種類。停止理由は、参照手順に残る旧指示（FAIL は必ず直す／提出条件は FAIL 0／即修正）と新契約の矛盾 | short-pipeline・longform・notation を新契約に統一。SKILL.md の「lint の読み方」を書き直し。JSON にも注意を同梱。部分操作・後続文の否認・半角濁点などを改善 |
| 9 | 契約と衝突する記述があと 2 箇所（revision.md の「新しい FAIL が無いこと」、ai-tells.md の C01 処方） | 修正文案どおり置換。保証表現（WARN になる）を「検知できた場合は」に。部分操作・否定の射程・発話を導く地の文を改善 |
| 10 | **出荷可**。契約矛盾なし。残る誤 FAIL は前後を読めば判断でき、新契約で扱える | 出荷後でよいとされた点のうち、別文で残字を示す部分操作・区間の表記（東京―名古屋）・表現の統一を実施 |

8 回目は一度 Codex の利用上限で実行できず、統括者が自前で 53 文を探索した（誤 FAIL 0 件）。その後に利用可能になり、8〜10 回目を実施した。

誤 FAIL の回帰は `scripts/tests/test_lint_false_fail_guard.py`（FAIL にしてはいけない文 214 件、FAIL にし続ける基本形 62 件。Codex の 4〜10 回目の入力をすべて含む）。C 群・N 群を触ったら、まずこのテストを見る。

## 残っている課題

- **字下げなしの原稿の中の作中文書**（チャット・メールの本文）。字下げで見分けられないので、「…」1 個や「！」の後の空白を N01・N03 FAIL にする。導入文（チャットを開いた）から範囲を認識するか、範囲を明示する記法を設ける。現状は運用契約（FAIL も読んで確かめる）で受ける。
- 文脈判定の取りこぼしは原理的に残る。C 群・N 群を触ったら `test_lint_false_fail_guard.py` を先に見て、見つけた誤 FAIL は必ずそこへ足す。
- 評価は 4 組・各 1 試行・同一モデル。結びの文言を直した後の再評価、`evals/evals.json` の 9 件での複数試行は未実施。
- lint の警報線のうち、現代エンタメ・Web 向けの列は未較正（`thresholds.json` の `calibrated`）。
- `references/prose-core.md` は毎回読む設計で約 380 行ある。短い対比例（悪い例）を残しているのは意図した判断だが、読み込み量と誘発のトレードオフは未検証。
- `references/notation.md` の [未]・[二] の項目。投稿・応募の前に公式情報で確かめること。
- `references/revision.md` の処置記録の例に、理由つきで keep した FAIL が残る例を足すと、運用が伝わりやすい。
