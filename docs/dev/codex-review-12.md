**修正は大部分で有効ですが、公開前に塞ぐべき穴が残っています。** ファイルは変更していません。Python 3.10 が実行でき、書き込み不要の既存108テストと、番人の FAIL 禁止214例・維持62例は通りました。全235テストの再実行はしていません。以下の追加ケースも実行確認しました。評価器の書き込みはメモリ上に差し替えています。

1. **高：generate を経由しなければ、改変された応答を元の生成条件の成果として採用する〔指摘1が未完〕**

   [run_blind_eval.py:389](../../evals/run_blind_eval.py:389)、[同:481](../../evals/run_blind_eval.py:481)。

   再現手順：generate 完了後、`gen/.../with.md` を別の本文へ変更し、**generate を挟まず** judge → report を実行する。

   `response_sha` を照合するのは `reusable()` だけです。judge は変更後の本文を判定し、report も採用します。保存された生成ハッシュと本文が不一致でも、元の tree・generator の結果として集計できます。

   **修正：** 生成記録の検証を共通化し、generate・judge・report で使用する。本文ハッシュ、依頼ハッシュ、source・tree・generator と実行条件の一致を必須にする。

2. **中：report が判定コマンドの不一致と、判定本文の変更を見逃す〔指摘1が未完〕**

   [run_blind_eval.py:359](../../evals/run_blind_eval.py:359)、[同:496](../../evals/run_blind_eval.py:496)。

   再現ケース：

   - `run.json` の `judge.command` と meta の `judge` を異なる値にする。report は成功し、別コマンドの判定を現在の条件として掲載する。
   - `order1.md` の選好を B から A に変更し、meta を残す。再開も report も B を採用する。

   前者は、判定コマンドを変更して再実行し、一部の meta 更新後に中断した場合にも起こり得ます。`judgment_valid()` のコマンド検証を report が使っていません。

   **修正：** report でも判定コマンドを照合し、混在を拒否する。判定本文にもハッシュを保存し、変更された本文と古い meta の組を無効にする。

3. **中：未閉鎖コードフェンス内の選好を採用する〔指摘8が未完〕**

   [run_blind_eval.py:341](../../evals/run_blind_eval.py:341)。

   次はどちらも `None` ではなく `"A"` を返します。

   ```python
   parse_preference("引用:\n```\n選好: A")
   parse_preference("引用:\n~~~\n選好: A")
   ```

   閉じたフェンスだけを正規表現で除去しているためです。インデントされたコード行 `"引用:\n    選好: A"` も採用されます。

   **修正：** 行単位でフェンスの開始・終了を追跡し、未閉鎖なら末尾までコード扱いにする。フェンス文字・長さと、インデントコードも扱う。

4. **中：沈黙行が密度の分母だけを増やし、診断を消す〔指摘6の修正による不整合〕**

   [novel_lint.py:440](../../scripts/novel_lint.py:440)、[同:1028](../../scripts/novel_lint.py:1028)。

   `entertainment / short` で再現：

   ```python
   s = "彼は歩いた。\n" * 100 + "彼は黙った……。\n" * 10
   # 680字、K02 WARN：10回、14.71/1000字

   s += "……\n" * 1000
   # 2680字、K02 が消える
   ```

   沈黙行は `total_chars` に入りますが、`all_lines()` には入りません。リーダーを1000回増やしても、検出回数は10回のままです。

   また、`"寂しかった。\n"` では K00 が出ますが、`"……\n"*300` を追加すると消えます。全文が600字を超えて短文側の処理から抜け、地の文側では分母不足で飛ばされるためです。

   **修正：** 文・段落統計とは別に、字数対象の沈黙行を全文検索にも保持する。K00／密度の切り替えは各ルールの分母で判断する。M01 が沈黙行を本文と認める変更自体は問題ありません。

5. **中：記号だけの文書単独では、まだ N07 が出ない〔指摘6が未完〕**

   [novel_lint.py:1496](../../scripts/novel_lint.py:1496)。

   ```text
   ＞**！**
   ```

   実際の結果は **M01 FAIL のみ、N07 なし**です。行走査へ変更しても、`total_chars == 0` では `check_notation()` 自体を呼びません。追加テストには前後の本文があるため、この経路を通りません。

   **修正：** N07 の走査を本文字数のガードから独立させる。上記の単独入力をテストに追加する。

6. **中：ログ欠落を「該当なし」と報告する〔指摘7の追加監査に穴〕**

   [run_blind_eval.py:518](../../evals/run_blind_eval.py:518)。

   ログを含まない公開済み `docs/dev/eval-round2/r2-*` が、そのまま再現入力です。3本ともログ0件なのに、`audit_logs()` は `[]` を返し、report は「該当なし」と表示します。

   **修正：** 「点検済み・該当なし」「該当あり」「ログ欠落・確認不能」を区別する。判定ログも `rglob()` で見つかったものだけでなく、必要な各判定から期待パスを列挙する。

   隔離の但し書きと全系統の入力ルート検査は妥当です。ただし、`evals/README.md` 冒頭の「生成役が過去の結果を読めないように」は「過去の結果をスキルの写しへ含めないため」に直すと説明が揃います。

前回の残りの項目：

- **2：問題なし。** 非ゼロ終了・解析不能・`meta.ok == false` は採用されません。
- **3：問題なし。** `＞` 単独行を含め、導入の持ち越しを止めています。
- **4：問題なし。** 前回の文字欠落・印残留と、同じ行数で文書／地の文を交換した追加ケースを拒否しました。
- **5：問題なし。** 見出し除去後も、全空行方針で文書境界を保持します。
- **9：問題なし。**
- **10：問題なし。**

`document_between()` は明示文書がなければ常に偽です。明示文書のない番人276例について、HEAD版と修正版の推定文書範囲・同一文末連続数を比較し、変化はありませんでした。

既存記録は、**旧実行器による探索記録として保存・公開して構いません。** 18応答のハッシュ、18判定の保存依頼文と現在の再構築依頼文、meta の選好と判定本文はすべて一致しました。ただし、後付けハッシュは実行当時の完全性を遡って保証しません。終了コードとログ点検は、公開ファイルだけでは追認できません。

README の現在の説明に、次を追記すると適切です。

> これらは旧実行器による探索的評価の移行記録であり、修正後の実行器の動作検証結果ではない。後付けハッシュは移行時点の保存ファイル間の整合性を示す。終了コードとログ点検は保管中の原ログに基づき確認したが、原ログは非公開のため、公開資料だけでは追認できない。

当時の応答・依頼文・成功試行との対応を原ログで確認できるなら、一律の再実行は不要です。対応が確認できない判定は当該作品対の両提示順を再判定し、生成条件を確認できない応答は当該作品対を両系統とも再生成して再判定してください。修正後の実行器を検証した実績が必要なら、既存記録とは別の run で実施します。

公開判定：**条件つき可（上記6件の修正と再現テストの追加が条件）。**