**現状のままの出荷は見送る判断です。**  
前回の典型例は改善しましたが、正しい本文への誤FAILと、必須資料を欠いて成功するパック生成が残っています。  
Python 3.12.14で標準入力CLIを66回実行し、新規本文32件・追加境界ケース10件とメモリ上のパック生成を検証しました。  
書き込みを伴わない既存テスト127件は全通過。ファイルは変更していません。

`python` がPATHに無かったため、実在するPython実行ファイルを指定して `-X utf8 scripts/novel_lint.py - --json` を実行しました。以下の本文再現はすべて実行済みで、`\n` は改行です。

**解消した指摘**

- TOP 1：SKILLの要約とcoreで、語り手の知識・人物の誤認・語り手固有の結びを認める説明が整いました。
- TOP 2：段落比較は同じ情報になり、writerの例も直言と交渉で進む場面へ変更されています。
- TOP 3：回転速度だけで漏水を否定する例は撤去されています。
- 1-1：視点人物と語り手の知識を区別しています。
- 1-2：同じ場面での和解、協働・交渉による解決を認めています。
- 2-4：空の洗濯機を回す旧例は撤去され、理解と不満を分けた結びになりました。
- 5-7：旧630字ケース・新規780字ケースとも、flashではK08の密度WARNが消え、K00の回数表示になりました。
- 新②：閏年でない2月29日、13月、0日を、曜日なしでもC08で検出しました。
- 新⑦：keepを声・意味・速度・修正範囲で判断し、普通の頷きや発話タグも残せます。
- 新⑧：トロ箱の修飾と、工程の所要時間を足す説明が修正されています。
- 評価・結びの過剰指示：笑い・照れ・静かな収束を認め、末尾あり／なしで比較する説明になりました。
- 評価・短文とメトロノーム：平均文長を上げる命令に戻っていません。
- 評価・直言と説明台詞：新しい認識や関係を加える直言を残す方針を維持しています。
- 評価・修正範囲と依頼4の記述：範囲遵守を維持し、READMEも「理由なし」から個々のヒットとの対応不足へ修正されています。

**出荷を止める残存問題**

**1. TOP 5／新①：部分的――C群に誤FAILが残り、訂正処理でも増えています。**

場所：[novel_lint.py:881](scripts/novel_lint.py:881)、[同:905](scripts/novel_lint.py:905)、[同:926](scripts/novel_lint.py:926)。

| 実行した入力 | 結果 |
|---|---|
| `「さようなら」は四文字ではありません。` | C01 FAIL、終了1 |
| `「こんにちは」は四文字でしょうか。` | C01 FAIL、終了1 |
| `「こんにちは」は四文字ではなく、六文字でもない。` | 否定している「六文字」をC01 FAIL |
| `「おかえり」は三文字ではなく、五文字だろうか。` | 疑問をC01 FAIL |
| `「いや」。三文字の言葉を探した。` | 別文の対象を結びつけてC01 FAIL |
| `「よし」は三拍ではありません。` | C03 FAIL、終了1 |

前回の再現例は改善しました。しかし訂正後の数を拾うと、後続の否定・疑問を確認せず `assert` に戻しています。

**修正案：** 数・単位・対象・後続節を一組として再判定する。敬体の否定・疑問も扱い、別文との対応が不確かな場合はWARNまでにする。訂正後の単位も保持する必要があります。

**2. 5-2：部分的――N05の対象拡大で、仕様どおりの本文がFAILになります。**

場所：[novel_lint.py:494](scripts/novel_lint.py:494)。仕様は[notation.md:135](references/notation.md:135)。

```text
　校門には誰もいなかった。
【立入禁止】の札が揺れていた。
```

```text
　彼は足を止めた。
〈帰れ〉と書いてある。
```

ともに2行目を「字下げの混在」としてN05 FAIL、終了1。仕様とexportは `【〈` 始まりを字下げ不要としますが、lintの除外集合にありません。従来の3段落条件を2段落へ広げた影響が出ています。

**修正案：** count/export/lintで段落分類と字下げ対象を共通化する。なお、通常の2段落混在と、1段落への明示的な `fullwidth／none／export` 契約は正しく検査できました。

**3. 5-1：部分的――日本語字種による本文の脱落が残っています。**

場所：[novel_lint.py:352](scripts/novel_lint.py:352)。

- `Hello, world!` と `𠮷。` は、ともに本文0字・M01 FAIL・終了1。
- `　手紙には一行だけあった。\nMeet me at noon.` は、英文行が本文統計から消えます。
- count/exportでは英文も拡張漢字の行も `narration` です。

**修正案：** 日本語が含まれることを本文の条件にせず、見出し・既知の場面転換行を除外する方式に揃える。単独の `……` を除外する指定は、そのまま維持できます。

**4. TOP 6：部分的――必須資料が存在しない場合は、欠落したパックで成功します。**

場所：[build_context.py:218](scripts/build_context.py:218)、[同:239](scripts/build_context.py:239)。

メモリ上の第4章プロジェクトで、文体シートと第3章末状態を除くと、ブロック4・6のないパックを正常に返しました。存在する0〜6を予算削減で落とさない処理は通過しています。

**修正案：** 必須資料の存在・実質的な内容を生成前に検証し、不足は `PackError`／終了2にする。第1章の前章末状態など、適用されない項目だけ明示的に除外する。

**出荷後の修正に回せる問題**

**5. 新③：部分的――N08は改善しましたが、『』の分解はまだ不正確です。**

場所：[novel_lint.py:110](scripts/novel_lint.py:110)、[同:127](scripts/novel_lint.py:127)。

| 入力 | 実測 |
|---|---|
| `『旅』という雑誌を買った。` | 書名を1発話と計上 |
| `『おはよう』と言い、『おやすみ』と答えた。` | 2発話を1発話と計上 |
| `　母は『待って』と頼んだ。` | 0発話 |
| `　彼は『「待て」と言われた』と答えた。` | 外側の発話ではなく、内側の「待て」だけを除去 |

N08の順序・種類の不対応検出は通過しましたが、不対応行の統計を欠測として扱う仕組みはありません。

**修正案：** 括弧の構造解析を発話抽出にも使い、『』の用途を設定可能にする。曖昧な書名・発話や不対応部分は、不確実として統計から分ける。

**6. TOP 7／新⑤：部分的――所定形式のブロック除外は通過、未対応形式は黙って残ります。**

場所：[build_context.py:102](scripts/build_context.py:102)。

前回の補足漏出例、字下げした子項目、子項目内の章番号は正しく除外しました。呼称も `from_ch=0/2/4/9` を第1・2・3・4・5・10章で試し、章開始時点の値を選べています。

一方、第5章用に次を与えると、そのまま残ります。

```markdown
## 変化の記録
| 章 | 内容 |
| ch012〜 | FUTURE_TABLE |
```

**修正案：** 文書化した箇条書き形式以外を検出して通知・停止する。所定形式では通過しているため、これは入力形式の検証強化として扱えます。

**7. 新⑥：部分的――終了2は解消、完成形の予算計測は未完です。**

場所：[build_context.py:299](scripts/build_context.py:299)、[同:315](scripts/build_context.py:315)。

必須ブロックだけのメモリ上のパックで、予算467字・報告467字として成功しましたが、返却文字列は空白除外で**553字**でした。末尾のハッシュコメント86字が計測後に加わります。

**修正案：** ハッシュを含む最終文字列で削減・停止・報告を統一する。予算1字での `main()` 終了コードは2を確認しました。

**8. 5-6／新④：部分的――旧例と近似表示は改善、説明の「んだ」の取りこぼしは残ります。**

場所：[novel_lint.py:149](scripts/novel_lint.py:149)。

`たたんだ／つんだ／学んだ／並んだ` は `ta` に分類されました。一方、`今夜も遊ぶんだ。` と `君が望むんだ。` も `ending_share={"ta":1.0}` になります。

**修正案：** 判別不能な形を曖昧クラスにするか、統計JSONにも近似分類である旨を付ける。R03名への近似表示は追加済みなので、出荷停止までは求めません。

**9. TOP 9：部分的――固定件数は減りましたが、最低1件の要求が残ります。**

場所：[reader.md:41](references/roles/reader.md:41)、同51行。

「妨げなし」でも相対的に弱い段落を1つ必ず挙げます。改稿不要と区別する説明は改善ですが、一段落の掌編でも選出を強制します。

**修正案：** 比較材料がない場合は0件を認める。SKILLの「全部直さない」とcoreの固定頻度は解消しています。

**10. 3-3：未解消――出典を追えない仕様記載が残っています。**

場所：[notation.md:151](references/notation.md:151)、同153〜155・174〜176行。

アルファポリスの「出典ページ未特定」と、個別確認先のない媒体仕様が残っています。

**修正案：** 主張ごとの公式確認先を付け、特定できないものは「確認済み」から未確認へ戻す。今回は仕様自体の誤りを断定していません。

**11. TOP 10／評価の再現記録・重複説明：部分的。**

場所：[評価README:17](docs/dev/eval-round1/README.md:17)、[prose-core.md:36](references/prose-core.md:36)、[with_1.md:35](docs/dev/eval-round1/with_1.md:35)。

完全な依頼文と判定プロンプトは保存されました。ただし生成時のスキル版は時期の説明にとどまり、実際の読込資料・ツール実行記録まで再現できません。平均文長の根拠となる3本文と生成条件も、このリポジトリから再現できる形では揃っていません。重複説明については規則があり、旧評価で指摘された適用漏れへの改善実証は未追加です。

**修正案：** 次回評価から版のハッシュ・読込資料・ツール記録を保存し、重複説明の適用を確認する。旧評価稿は証跡として残してよく、今回の出荷条件として再評価を追加要求するものではありません。

指定された意図的判断――短い対比例、coreの毎回読込と分量、括弧なしの単独 `……` の除外、評価規模と結び変更後の未再評価――は未解消件数に含めません。TOP 8・7-2の該当部分も対象外です。

**出荷前に必要なのは、C群・N05・本文判定の誤FAILと、必須資料欠落時のパック生成停止です。** 既存127件が通っていても、今回実行した正しい入力を保護できていないため、現在の「FAILは直す」という運用のまま出荷することには反対です。